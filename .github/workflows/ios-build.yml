name: iOS Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Install dependencies
        run: |
          brew install cmake ninja pkg-config

      - name: Fetch MoltenVK iOS dependencies
        run: |
          cd external/MoltenVK
          ./fetchDependencies --ios

      - name: Build MoltenVK for iOS (framework + shader converter toolkit)
        run: |
          cd external/MoltenVK
          make ios

      - name: Cache vcpkg binary packages (incremental across runs)
        uses: actions/cache@v6
        with:
          path: |
            ${{ github.workspace }}/external/vcpkg/installed
            ${{ github.workspace }}/external/vcpkg/packages
            ${{ github.workspace }}/**/vcpkg_installed
          key: vcpkg-bin-${{ runner.os }}-arm64-ios-${{ hashFiles('vcpkg.json') }}
          restore-keys: |
            vcpkg-bin-${{ runner.os }}-arm64-ios-

      - name: Setup vcpkg
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgGitCommitId: 77df67cfff9c12ccfdb52284e07c87c75092f723
          vcpkgJsonGlob: 'vcpkg.json'
          runVcpkgInstall: true
          vcpkgDirectory: '${{ github.workspace }}/external/vcpkg'

      - name: Configure CMake (iOS) - Ninja (fast build)
        run: |
          cmake -B build-ios-ninja \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_OSX_SYSROOT=iphoneos \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=17.0 \
            -GNinja \
            -DVCPKG_TARGET_TRIPLET=arm64-ios \
            -DVCPKG_TARGET_ARCHITECTURE=arm64 \
            -DMVK_IOS=ON \
            -DMVK_MACOS=OFF \
            -DMVK_BUILD_SHADER_CONVERTER_TOOL=OFF \
            -DCMAKE_TOOLCHAIN_FILE=$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake

      - name: Build Vita3K iOS (Ninja)
        run: |
          cmake --build build-ios-ninja --config Release --target vita3k -j$(sysctl -n hw.logicalcpu) -- -v 2>&1 | tail -100

      - name: Configure CMake (iOS) - Xcode (for IPA)
        run: |
          cmake -B build-ios-xcode \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_OSX_SYSROOT=iphoneos \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=17.0 \
            -GXcode \
            -DVCPKG_TARGET_TRIPLET=arm64-ios \
            -DVCPKG_TARGET_ARCHITECTURE=arm64 \
            -DMVK_IOS=ON \
            -DMVK_MACOS=OFF \
            -DMVK_BUILD_SHADER_CONVERTER_TOOL=OFF \
            -DCMAKE_TOOLCHAIN_FILE=$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake

      - name: Archive for IPA
        run: |
          xcodebuild -project build-ios-xcode/Vita3K.xcodeproj \
            -scheme vita3k \
            -configuration Release \
            -archivePath build-ios-xcode/Vita3K.xcarchive \
            archive

      - name: Export IPA
        run: |
          xcodebuild -exportArchive \
            -archivePath build-ios-xcode/Vita3K.xcarchive \
            -exportPath artifacts \
            -exportOptionsPlist ios/ExportOptions.plist || true

      - name: Package artifacts
        run: |
          mkdir -p artifacts
          find build-ios -name "*.dylib" -o -name "vita3k*" | head -10 > artifacts/files.txt || true

      - name: Save vcpkg binary cache (even on failure)
        if: always()
        uses: actions/cache/save@v6
        with:
          path: |
            ${{ github.workspace }}/external/vcpkg/installed
            ${{ github.workspace }}/external/vcpkg/packages
            ${{ github.workspace }}/**/vcpkg_installed
          key: vcpkg-bin-${{ runner.os }}-arm64-ios-${{ hashFiles('vcpkg.json') }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: vita3k-ios
          path: artifacts/
