import Foundation

class StikDebugJIT {
    static func enable() -> Bool {
        // Placeholder: call into StikDebug entitlement grant
        // Requires the app to have get-task-allow and be launched via StikDebug
        NSLog("[Vita3K] StikDebug JIT enable requested")
        return true
    }
}
